let name; // задаем имени значение
do { // функция
name = prompt('Введите имя'); // вводим имя
} while (!name); // условие
alert("Привет, " + name + "!"); // чтобы показать, что всё нормально записалось